/*
Домашнее задание 1: Предсказание вывода

Перед запуском — письменно запишите (в комментариях) ожидаемый вывод
для каждого примера, объяснив каждую строку. Затем запустите и сверьтесь.

═══════════════════════════════════════════════════════════
Пример A: slicing_task
═══════════════════════════════════════════════════════════

    a := [...]int{0, 1, 2, 3}
    x := a[:1]
    y := a[2:]

    x = append(x, y...)
    x = append(x, y...)

    fmt.Println("a:", a)
    fmt.Println("x:", x)

Ответьте на вопросы:
  1. После `x := a[:1]`  — len(x)=?, cap(x)=?, шарит ли x массив a?
  2. После первого append — произошла ли реаллокация? Почему? Что в a[1..3]?
  3. После второго append — реаллокация? Почему?
  4. Какой финальный вывод a и x?

Запишите ответы ниже в комментарии:
  // 1. ...
  // 2. ...
  // 3. ...
  // 4. ...

═══════════════════════════════════════════════════════════
Пример B: range_task
═══════════════════════════════════════════════════════════

    var x = []string{"A", "M", "C"}
    for i, s := range x {
        print(i, s, ",")
        x[i+1] = "M"
        x = append(x, "Z")
        x[i+1] = "Z"
    }

Ответьте на вопросы:
  1. Сколько итераций выполнит цикл? Почему?
  2. На какой итерации произойдёт реаллокация слайса x?
  3. Что будет выведено на каждой итерации?

Запишите ответы ниже в комментарии:
  // 1. ...
  // 2. ...
  // 3. ...
*/

package main

import "fmt"

func exampleA() {
	fmt.Println("=== Пример A ===")
	a := [...]int{0, 1, 2, 3}
	x := a[:1]
	y := a[2:]

	x = append(x, y...)
	x = append(x, y...)

	fmt.Println("a:", a)
	fmt.Println("x:", x)
}

func exampleB() {
	fmt.Println("=== Пример B ===")
	var x = []string{"A", "M", "C"}
	for i, s := range x {
		print(i, s, ",")
		x[i+1] = "M"
		x = append(x, "Z")
		x[i+1] = "Z"
	}
	println()
}

func main() {
	exampleA()
	exampleB()
}
