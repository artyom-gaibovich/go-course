/*
Домашнее задание 2: Корректный broadcast-нотификатор.

Реализуйте Notifier, который умеет оповещать ВСЕХ подписчиков МНОГОКРАТНО
(в отличие от одиночного close(ch), который срабатывает лишь один раз).

  - Subscribe() <-chan struct{} — вернуть канал, на котором подписчик получит
    сигнал при каждом Broadcast.
  - Broadcast()                 — разбудить всех текущих подписчиков.

Подумайте, почему здесь не обойтись одним каналом: close можно вызвать только
раз, а запись будит лишь одного. Приходит на помощь приём «текущий канал»:
храните один общий канал, при Broadcast закрывайте его и заводите новый, чтобы
будущие Broadcast снова работали. Защищайте состояние мьютексом.

Критерии:
  [ ] Broadcast будит всех подписчиков, оформивших подписку до него;
  [ ] Notifier работает при повторных Broadcast;
  [ ] нет гонок (go run -race) и утечек горутин.
*/

package main

import "sync"

type Notifier struct {
	mu sync.Mutex
	// TODO: текущий канал оповещения
}

func NewNotifier() *Notifier {
	// TODO
	return nil
}

func (n *Notifier) Subscribe() <-chan struct{} {
	// TODO
	return nil
}

func (n *Notifier) Broadcast() {
	// TODO: закрыть текущий канал и завести новый
}

func main() {
	// TODO: подписать несколько горутин, вызвать Broadcast дважды, дождаться сигналов
}
