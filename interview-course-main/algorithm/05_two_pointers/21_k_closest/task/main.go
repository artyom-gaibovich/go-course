package main

// Задача: K ближайших элементов
//
// Дан отсортированный по возрастанию список целых чисел arr, индекс элемента index и целое число k.
// Необходимо вернуть в любом порядке k чисел из списка, которые являются ближайшими по значению
// к элементу arr[index].
//
// Ограничения:
// - Размер списка 1 <= N <= 10^6
// - Элементы списка: -10^9 <= arr[i] <= 10^9
// - Число 0 <= k <= N
// - Индекс элемента 0 <= index < N
// - Среди двух одинаково отдалённых элементов выбирается больший
//
// Примеры:
// findKClosest(arr=[2, 3, 5, 7, 11], index=3, k=2) → [5, 7] или [7, 5]
// findKClosest(arr=[4, 12, 15, 15, 24], index=1, k=3) → [12, 15, 15]

func findKClosest(arr []int, index int, k int) []int {
	// TODO: реализуйте функцию
	return nil
}

func main() {
	result := findKClosest([]int{2, 3, 5, 7, 11}, 3, 2)
	println("Test 1:", result) // [5, 7] или [7, 5]

	result = findKClosest([]int{4, 12, 15, 15, 24}, 1, 3)
	println("Test 2:", result) // [12, 15, 15]
}
