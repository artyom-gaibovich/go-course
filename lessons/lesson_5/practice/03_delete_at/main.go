/*
Задание 3: Безопасное удаление элемента

Реализуйте функцию:

    DeleteAt(s []int, i int) []int

Которая удаляет элемент по индексу i.

Требования:
  - Паника при i < 0 или i >= len(s).
  - Используйте append(s[:i], s[i+1:]...) для сдвига.
  - Обнулите последний элемент (s[len(s)-1] = 0) — важно для ссылочных типов.
  - Верните укороченный слайс.

Проверьте три случая:
  - Удаление из середины: DeleteAt([]int{1,2,3,4,5}, 2) → [1 2 4 5]
  - Удаление первого:     DeleteAt([]int{1,2,3,4,5}, 0) → [2 3 4 5]
  - Удаление последнего:  DeleteAt([]int{1,2,3,4,5}, 4) → [1 2 3 4]
*/

package main

import "fmt"

// TODO: реализуйте DeleteAt
func DeleteAt(s []int, i int) []int {
	// TODO: проверка границ
	// TODO: удаление через append
	// TODO: обнуление хвостового элемента
	return nil
}

func main() {
	fmt.Println(DeleteAt([]int{1, 2, 3, 4, 5}, 2)) // [1 2 4 5]
	fmt.Println(DeleteAt([]int{1, 2, 3, 4, 5}, 0)) // [2 3 4 5]
	fmt.Println(DeleteAt([]int{1, 2, 3, 4, 5}, 4)) // [1 2 3 4]
}
