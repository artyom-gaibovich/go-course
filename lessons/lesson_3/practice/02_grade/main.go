/*
Задание 2: Калькулятор оценок

По баллу (0–100) выведи оценку по системе:
  90–100 → "A (Отлично)"
  75–89  → "B (Хорошо)"
  60–74  → "C (Удовлетворительно)"
  40–59  → "D (Слабо)"
  0–39   → "F (Провал)"

Для баллов вне диапазона [0, 100] выводи: "Некорректный балл"

Реализуй с помощью switch без выражения (switch { case ...: }).
Протестируй на значениях: 95, 82, 67, 45, 20, -5, 110.
*/

package main

import "fmt"

func grade(score int) string {
	switch {
	// TODO: добавь case для каждого диапазона
	default:
		return "Некорректный балл"
	}
}

func main() {
	scores := []int{95, 82, 67, 45, 20, -5, 110}
	for _, s := range scores {
		fmt.Printf("Балл %3d → %s\n", s, grade(s))
	}
}
