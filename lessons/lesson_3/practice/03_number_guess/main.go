/*
Задание 3: Угадай число

Программа загадывает фиксированное число (например, 42).
Пользователь вводит числа в цикле, пока не угадает.
После каждой попытки выводи подсказку: "Больше", "Меньше" или "Угадал!".
При правильном ответе — сообщи количество попыток и выйди из цикла.

Для чтения ввода используй fmt.Scan(&n).

Пример сессии:
  Введи число: 50
  Меньше
  Введи число: 30
  Больше
  Введи число: 42
  Угадал! Попыток: 3

Усложнение (по желанию): ограничь количество попыток до 7.
*/

package main

import "fmt"

func main() {
	secret := 42
	attempts := 0

	for {
		var guess int
		fmt.Print("Введи число: ")
		fmt.Scan(&guess)
		attempts++

		switch {
		// TODO: сравни guess с secret, выведи подсказку или поздравление
		}
	}
}
