/*
Задание 1: Итерация и модификация массива

Дан массив [5]int{1, 2, 3, 4, 5}.

Сделайте две попытки удвоить все элементы:

Попытка 1 (НЕПРАВИЛЬНАЯ):
  - Используйте `for i, v := range arr`
  - Внутри цикла напишите `v *= 2`
  - Выведите массив — покажите, что он НЕ изменился
  - Добавьте комментарий: почему v не меняет массив?

Попытка 2 (ПРАВИЛЬНАЯ):
  - Используйте индексацию `arr[i] *= 2`
  - Выведите массив — покажите, что он изменился

Ожидаемый вывод:
  после попытки 1: [1 2 3 4 5]
  после попытки 2: [2 4 6 8 10]
*/

package main

import "fmt"

func main() {
	arr := [5]int{1, 2, 3, 4, 5}

	// Попытка 1: через v в range — НЕ изменяет массив
	for i, v := range arr {
		_ = i
		_ = v
		// TODO: v *= 2  ← это изменяет только локальную копию
	}
	fmt.Println("после попытки 1:", arr)

	// Попытка 2: через индексацию — изменяет массив
	for i := range arr {
		_ = i
		// TODO: arr[i] *= 2
	}
	fmt.Println("после попытки 2:", arr)
}
