/*
Задание 1: Синхронный результат из горутины.

Напишите функцию compute(ch chan<- int, x int), которая в отдельной горутине
считает x*x (с искусственной задержкой через time.Sleep) и пишет результат в
канал ch. В main создайте канал, запустите compute и синхронно получите
результат, затем напечатайте его.

Требования:
  - сигнатура compute использует однонаправленный тип chan<- int;
  - канал создаётся в main;
  - результат читается из канала в main.
*/

package main

import (
	"time"
)

func compute(ch chan<- int, x int) {
	time.Sleep(1 * time.Second)

	// TODO: задержка + запись x*x в канал
}

func main() {

	// TODO: создать канал, запустить горутину, прочитать и напечатать результат
}
